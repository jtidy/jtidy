<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:j="jelly:core" xmlns:u="jelly:util">
  <goal name="rebuild-all" prereqs="pom,clean,ant,dist" />
  <goal name="nightly" prereqs="multiproject:clean,multiproject:install,dist,multiproject:site,copy-snapshots, site:sshdeploy" />
  <preGoal name="dist:prepare-src-filesystem">
    <attainGoal name="ant" />
  </preGoal>
  <postGoal name="dist:prepare-src-filesystem">
    <copy todir="${maven.dist.src.assembly.dir}">
      <fileset dir=".">
        <include name="checkstyle.xml" />
      </fileset>
    </copy>
  </postGoal>
  <goal name="copy-snapshots">
    <mkdir dir="${maven.build.dir}/docs/nightly" />
    <copy todir="${maven.build.dir}/docs/nightly">
      <fileset dir="${maven.build.dir}/distributions" />
      <fileset dir="../jtidyservlet/target">
        <include name="*.jar" />
      </fileset>
      <fileset dir="../jtidyservlet-webapp/target">
        <include name="*.war" />
      </fileset>
      <fileset dir="${maven.build.dir}/distributions" />
    </copy>
    <mkdir dir="${maven.build.dir}/docs/maven" />
    <copy file="${maven.build.dir}/${pom.artifactId}-${pom.currentVersion}.jar" todir="${maven.build.dir}/docs/maven/${pom.groupId}/jars" />
  </goal>
</project>
